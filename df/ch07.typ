#import "../../libs.typ": *

#show: ilm.with(
  title: [Electric Circuit],
  date: datetime.today(),
  author: "Junfeng Lve",
  abstract: [Study notes on Chapter 7, Latches, Flip-Flops and Timers of _Digital Fundamentals_, Thomas L. Floyd.],
)
#show: setup

= 555 Timer

#overview[555定时器的核心作用][
  555定时器是一种把比较、锁存、放电与输出级集成在一起的中规模集成器件，既可产生单次定时脉冲，也可自激产生连续方波。
]

555定时器之所以常用，是因为它把“阈值检测 + 状态记忆 + 电容充放电控制”做成了一个完整模块。设计时通常只需外接电阻、电容，就能得到比较稳定的时间间隔或振荡信号。

#definition[内部工作机理][
  555定时器内部可分成四个关键部分：
  + 三个串联电阻构成分压网络，提供 $1 / 3 V_"CC"$ 与 $2 / 3 V_"CC"$ 两个基准电平；
  + 两个电压比较器分别检测触发端与阈值端；
  + 一个RS锁存器负责保存当前状态；
  + 一个放电晶体管与输出级根据锁存器状态决定电容是充电还是放电。
]

当触发端电压低于 $1 / 3 V_"CC"$ 时，下比较器置位锁存器，输出端变为高电平，同时放电晶体管截止，外接电容开始充电。当阈值端电压升到高于 $2 / 3 V_"CC"$ 时，上比较器复位锁存器，输出端变为低电平，放电晶体管导通，电容迅速放电。

#note[两个门限电平的意义][
  + $1 / 3 V_"CC"$ 决定“开始一个新的定时过程”；
  + $2 / 3 V_"CC"$ 决定“结束这次定时过程”；
  + 因为两个门限不同，所以电路天然具有回差，不容易在临界点附近抖动。
]

内部功能关系可概括为：比较器负责判断，锁存器负责记忆，放电管负责改变电容电压的演化方向，因此555定时器本质上是一个把模拟电压变化转换成数字高低状态的时序器件。

内部结构示意如图所示。

#image("/assets/image.png")

= 引脚速记

#definition[555的8个引脚][
  555定时器的常见8个引脚可按功能分为三组：
  + 电源相关：$V_"CC"$、GND；
  + 状态控制相关：TRIG、THRES、RESET、CTRL；
  + 输出执行相关：OUT、DISCH。
]

#note[逐个记忆][
  + 1脚 GND：接地，是整个芯片的参考地；
  + 2脚 TRIG：触发端，低于 $1 / 3 V_"CC"$ 时触发定时开始；
  + 3脚 OUT：输出端，给出高低电平脉冲或方波；
  + 4脚 RESET：复位端，低电平有效，能强制输出回到初始状态；
  + 5脚 CTRL：控制电压端，可外加电压改变比较门限，平时常通过小电容接地以抗干扰；
  + 6脚 THRES：阈值端，高于 $2 / 3 V_"CC"$ 时结束本次定时；
  + 7脚 DISCH：放电端，内部接放电晶体管，用来给外接电容放电；
  + 8脚 $V_"CC"$：电源端。
]

#strategy[速记方法][
  可按引脚编号记成：

  $1$ 地，$2$ 触发，$3$ 输出，$4$ 复位，$5$ 控制，$6$ 阈值，$7$ 放电，$8$ 电源。
]

#remark[最该优先记住的四个引脚][
  做题和分析波形时，最关键的是 $2$ 脚 TRIG、$6$ 脚 THRES、$7$ 脚 DISCH、$3$ 脚 OUT。

  其中 TRIG 决定“什么时候开始”，THRES 决定“什么时候结束”，DISCH 决定“电容如何回到初态”，OUT 则直接反映整个定时器当前状态。
]

= 单稳态工作方式

#definition[单稳态的含义][
  单稳态电路只有一个稳定状态。555定时器在单稳态应用中，平时保持低电平稳定；一旦受到触发，就暂时输出一个高电平脉冲，持续一段时间后自动回到原稳定状态。
]

这种工作方式适合做延时、定宽脉冲、按键消抖和缺脉冲检测。它的关键在于：输出脉冲宽度不主要由触发信号本身决定，而主要由外接的 $R C$ 参数决定。

#method[单稳态的过程][
  + 初始时放电晶体管导通，定时电容电压近似为 $0$，输出为低电平；
  + 当触发端出现一个足够低的负脉冲，使其低于 $1 / 3 V_"CC"$ 时，锁存器被置位；
  + 输出跳变为高电平，放电晶体管截止，电容通过外接电阻充电；
  + 当电容电压升到 $2 / 3 V_"CC"$ 时，锁存器复位，输出重新变低，放电晶体管导通，电容放电，电路回到稳定状态。
]

若电容从 $0$ 通过电阻 $R$ 向电源 $V_"CC"$ 充电，则
$
  v_C(t) = V_"CC" (1 - e^(-t / (R C)))
$
单稳态脉冲结束时满足
$
  v_C(t_W) = 2 / 3 V_"CC"
$
代入上式可得
$
  t_W = R C ln 3 approx 1.1 R C
$

#result[单稳态脉冲宽度公式][
  对555定时器单稳态电路，输出高电平持续时间近似为
  $
    t_W approx 1.1 R C
  $
]

#remark[如何理解这个公式][
  脉冲宽度由“电容从 $0$ 充到 $2 / 3 V_"CC"$ 所需时间”决定，因此增大电阻或电容，都会使定时时间变长。
]

= 无稳态工作方式

#definition[无稳态的含义][
  无稳态电路没有稳定状态。555定时器在无稳态连接中，电容不断在两个门限电平之间往复充放电，于是输出端连续地在高、低电平之间翻转，形成周期性矩形波。
]

无稳态电路常用于时钟脉冲、闪光控制、音调发生和低频振荡。和单稳态相比，它不需要外部触发信号，接通电源后便会自动振荡。

#method[无稳态的过程][
  + 当输出为高电平时，放电晶体管截止，电容通过外接电阻充电；
  + 当电容电压升到 $2 / 3 V_"CC"$ 时，输出翻为低电平；
  + 此时放电晶体管导通，电容经放电回路放电；
  + 当电容电压降到 $1 / 3 V_"CC"$ 时，输出又翻为高电平；
  + 如此循环，形成周期振荡。
]

对教材中常见的连接方式，设充电通路中的两个电阻分别为 $R_A, R_B$，电容为 $C$，则电容由 $1 / 3 V_"CC"$ 充到 $2 / 3 V_"CC"$ 的时间为高电平持续时间：
$
  t_H = 0.693 (R_A + R_B) C
$
放电时电容由 $2 / 3 V_"CC"$ 降到 $1 / 3 V_"CC"$，对应低电平持续时间：
$
  t_L = 0.693 R_B C
$
因此振荡周期与频率分别为
$
  T = t_H + t_L = 0.693 (R_A + 2 R_B) C
$
$
  f = 1 / T approx 1.44 / ((R_A + 2 R_B) C)
$

#result[无稳态的主要公式][
  + 高电平时间：$t_H = 0.693 (R_A + R_B) C$
  + 低电平时间：$t_L = 0.693 R_B C$
  + 振荡周期：$T = 0.693 (R_A + 2 R_B) C$
  + 振荡频率：$f approx 1.44 / ((R_A + 2 R_B) C)$
]

若把占空比记为 $D$，则
$
  D = t_H / T = (R_A + R_B) / (R_A + 2 R_B)
$
可见在这种最基本连接中，总有 $D gt 1 / 2$。这说明输出高电平通常比低电平更长；若需要接近 $50 %$ 的占空比，往往要进一步改进充放电通路。

常见单稳态与无稳态外接方式如图所示。

#image("/assets/image-1.png")

#summary[555定时器的主线][
  + 看内部：比较器检测 $1 / 3 V_"CC"$ 与 $2 / 3 V_"CC"$，锁存器决定输出状态；
  + 看单稳态：一次触发产生一次宽度约为 $1.1 R C$ 的脉冲；
  + 看无稳态：电容在两个门限之间往复充放电，形成连续振荡；
  + 所有时间参数，本质上都来自电容充放电达到门限所需的时间。
]
